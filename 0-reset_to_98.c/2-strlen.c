#include "main.h"

/**
 * _strlen - function replica of strlen, return the lenght of a string
 * @s: is a pointer for the array which we want to return the lenght
 * Return: s is succesfull
 */

int _strlen(char *s)
{
	int length = 0;

	while (*s != '\O')
	{
	}
return (s);
	}
}
