#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char ch;
char CHM;
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
for (CHM = 'A'; CHM <= 'Z'; CHM++)
{
putchar(CHM);
}
{
putchar('\n');
}
return (0);
}
