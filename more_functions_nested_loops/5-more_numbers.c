#include <stdio.h>
#include "main.h"

/**
 * more_numbers - function to display number from 0 to 14
 *
 * Return: 0 for successful
 */

void more_numbers(void)
{
int ch;
for (ch = 0; ch <= 14; ch++)
_putchar (ch);
_putchar('\n');
}
