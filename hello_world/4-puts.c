#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
/*Using puts () */
puts("\"Programming is like building a multilingual puzzle");

/* return with 0 */
return (0);
}
